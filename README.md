# Front

Vue 3 + TypeScript + Vite, com layout **Sakai** (template oficial gratuito do PrimeVue).

```bash
cd front
npm install
npm run dev
```

Abre em http://localhost:5173

A API NestJS deve estar em http://localhost:3000. O Vite faz proxy de `/api` para a API.
